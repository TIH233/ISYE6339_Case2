{
  "eval_id": 1,
  "eval_name": "task3-clustering-setup",
  "prompt": "Let's start implementing Task 3 — the region clustering notebook. I want to cluster the NE counties into roughly 50 demand-balanced regions. Walk me through how to set up the notebook and write the first section that loads the freight flow data and computes county-level demand.",
  "assertions": [
    "Response invokes retriever or explicitly calls awk to fetch Task 3 description from Doc/Task.md before writing code",
    "At least one notebook cell shown is a Markdown cell containing a LaTeX equation ($ or $$)",
    "Data loading code uses pandas (read_parquet or read_csv), and computation code uses numpy — not mixed in the same cell",
    "A cache/serialization pattern is shown (np.save, joblib.dump, or Path.exists() guard)",
    "A sanity check is included (assert statement or print of shape/stats)"
  ]
}
